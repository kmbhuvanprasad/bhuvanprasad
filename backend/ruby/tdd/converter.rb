class Converter
	def convert(arabic)
	# 	if arabic==1
	# 	return "I"
	# elsif arabic==2
	# 	return "II"
	# else
	# 	return "III"
	result=0
	arabic.times==
	end
end
end

describe Converter do 
	describe "#convert" do
	it"should give I when given 1" do 
		#context
		c=Converter.new
		roman=c.convert(1)
		expect(roman).to eq "I"
	end
	it"should give II when given 2" do 
		c=Converter.new
		roman=c.convert(2)
		expect(roman).to eq "II"
	end
	it"should give III when given 3" do 
		c=Converter.new
		roman=c.convert(3)
		expect(roman).to eq "III"
	end

end
end
